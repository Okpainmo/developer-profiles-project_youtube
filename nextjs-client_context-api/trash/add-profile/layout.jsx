import React from 'react';

export const metadata = {
  title:
    'Add Profile | full stack project with the MERN stack, tailwind-css, and html.',
  description:
    'full stack project with the MERN stack, tailwind-css, and html.',
};

function AddProfilePageLayout({ children }) {
  return <>{children}</>;
}

export default AddProfilePageLayout;
